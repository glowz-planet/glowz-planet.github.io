# このリポジトリについて

夜職店舗のための **AI経営プラットフォーム「GLOWZ」** の公開サイトです。
GitHub Pages（`main` ブランチ）でそのまま配信しています。
→ https://glowz-planet.github.io/

いまはサーバーを使わない **静的HTML＋ダミーデータ**。
ビルドもパッケージもありません。HTMLを開けばそのまま動きます。

| 画面 | 場所 | `data-brand` |
|---|---|---|
| ブランド一覧（表紙） | `index.html` | `customer` |
| BUSINESS OS（店舗経営） | `business/index.html` | `business` |
| THERAPIST OS（キャスト） | `therapist/index.html` | `therapist` |
| CUSTOMER WEB（お客様） | `customer/index.html` | `customer` |

共通の中身は `assets/` にまとまっています。

| ファイル | 中身 |
|---|---|
| `assets/glowz.css` | デザイントークン（色・影・角丸・文字）と共通パーツ |
| `assets/glowz.js` | Canvasのグラフ、タブ切りかえ、小物 |
| `assets/icons.js` | アイコン |

---

## 名前は **GLOWZ**

**`Growz` と書かないこと。** `GLOWZ`（L）です。
"glow"（光る）＋ "grow"（育つ）を重ねた名前で、
タグラインの *Grow Together. Glow Forever.* が両方を出しています。

- サービス名・見出し・ロゴ：`GLOWZ`（大文字）
- サイト全体／会社まわりの呼び名：`Glowz Planet`
- パス・ID・クラス名：`glowz`（小文字）

`Growz`・`GLOWS`・`Glowz OS` などの揺れを見つけたら直してください。
`Grow Together` の `Grow` は **タグラインの単語なので、そのまま**です。

---

## 色は `data-brand` ひとつで切りかえる

`<html lang="ja" data-brand="therapist">` と書くだけで、そのページの色が
すべて変わります。

**ページ側に色を直接書かないこと。**
`#F2529C` のような生の値や、`style="color:…"` をHTMLに書くと、
ブランドを増やしたとき（GLOWZ Manager / Cast / CRM …）に
直す場所が散らばります。色は必ず `var(--…)` 経由で参照します。

| | `--bg-1` | `--bg-2` | `--bg-3` | `--accent` |
|---|---|---|---|---|
| `business` | `#F7F8FA` | `#E6E8EC` | `#C0C3C8` | `#3E8FD6` |
| `therapist` | `#FFF0F6` | `#FFD6E6` | `#D3D5DB` | `#F2529C` |
| `customer` | `#FFF7ED` | `#F1E6D3` | `#C8C8CF` | `#C9A24B` |

新しい色が要るときは、ページではなく `glowz.css` の
`[data-brand="…"]` に **3ブランドぶんまとめて** 足します。
1ブランドだけ足すと、他の2つで変数が未定義になって色が消えます。

**例外はシルバーだけ**です。`--sv-0` 〜 `--sv-5` は3色すべてに共通で入るので、
`data-brand` では切りかえません。ブランドガイドの「共通要素：Pearl・Silver・
Aurora・Glass」がそのままトークンになっています。

表紙（`index.html`）だけは3つのOSカードを並べるので、
`<html>` ではなく **カードごとに `data-brand`** を付けて色を出しています。
入れ子にしても、内側の指定が勝ちます。

---

## パールとオーロラはCSSで描く

デザイン見本のパール球とオーロラ背景は **AI生成の画像**でしたが、
ここでは CSS のグラデーションで作りなおしています。

- 拡大しても割れない（ロゴにもヒーローにも同じ `.pearl` を使える）
- ブランドごとに色が変わる（`--pearl-hi` 〜 `--pearl-sh`）
- 画像を1枚も読みこまないので、開いた瞬間から出ている

球に見せるには **ハイライト（左上）と反射光（下）の2つ**が要ります。
反射光を消すと、とたんに「ただの円」になります。

画像素材に差しかえたくなったら `.pearl` の `background` を
`url()` に替えるだけで、置き場所は変えずに済みます。

---

## 文字の大きさは `--fs` ひとつ

`glowz.css` の `font-size` はすべて `calc(◯px * var(--fs))` の形です。
`:root` の `--fs`（いま **1**）を変えると全体がまとめて変わります。
新しく文字を足すときも、この形にそろえてください。

かけていないのは **絵文字アイコン**だけです。入れものの幅が固定なので、
大きくすると枠から出ます。

---

## グラフはCanvasで自前に描く

要るのは「折れ線」「ドーナツ」「スパークライン」の3つだけなので、
Chart.js は入れていません（初回表示が重くなるため）。
**外部ライブラリは足さない**方針です。

**隠れている画面のCanvasは幅0になります。** そのまま描くと
半径がマイナスになって `arc()` が例外を投げるので、`fitCanvas()` が
`null` を返して呼ぶ側が黙って戻ります。画面を切りかえたあとに
`draw()` を呼びなおせば、そのとき正しい幅で描けます。
（THERAPIST のタブ切りかえで実際に起きました）

`devicePixelRatio` にも合わせてあります。合わせないと Retina で
グラフだけぼやけます。

---

## 写真の枠は `<img>` を足すだけで差しかわる

人物写真はまだありません。いまは `.av`（イニシャルをのせたパール球）と
`.visual`（ヒーローの仮置き）で埋めてあります。

```html
<span class="av"><img src="…"></span>   <!-- これで写真になる -->
```

素材を入れるときは **商用利用可・再配布可**のものだけにしてください。
GitHub Pages に置く＝誰でもダウンロードできる状態になります。

---

## お客様向けサイトは年齢確認を通す

`customer/index.html` は成人向けサービスの入口なので、最初に年齢確認を出します。

`sessionStorage` にしているのは、**タブを閉じたらまた確認するため**です。
`localStorage` だと共用端末で次の人にも「確認済み」が引きつがれます。
ここを軽くしないでください。検査スクリプトも `#yes` を押してから中を見ます。

---

## Appleに出すときの注意

**お客様向け（CUSTOMER）はネイティブアプリにできません。**
Appleは成人向けサービスの予約・紹介アプリを審査で通しません
（App Store Review Guideline 1.1.4 / 5.0）。
デザイン見本が CUSTOMER だけ「Web / PWA」になっているのは正しい判断です。

店舗管理側（BUSINESS / THERAPIST）は業務ツールなので余地がありますが、
どちらにせよ **まずPWAで作る**のが確実です。

---

## 検証（変更したらかならず走らせる）

```
node tools/check-glowz.mjs      # 終了コード 0 で合格
```

**HTML・CSS・JSをさわったら、報告する前にこれを通してください。**
目で見て「直った」と思ったあとにかけて、隠れた画面の Canvas が
幅0で例外を投げているのが実際に見つかっています。

4画面 × 2サイズ（1440 / 390）を実際に開いて、次を見ます。

| | 見つかるもの |
|---|---|
| JSエラー | 描画中の例外 |
| 横はみ出し | 画面より広い要素 |
| Canvasが空 | 幅0のまま描けていないグラフ |
| タブ切りかえ | 画面が1まいずつ入れかわっているか |

Googleフォントは取りに行けない環境があるので、その失敗だけは対象外です。
Chromium の場所が違う環境では `CHROME=… node tools/check-glowz.mjs` で
上書きできます。スクリーンショットは `/tmp/glowz-shots`（`OUT` で変更可）。

**画面を1つ足したら `tools/check-glowz.mjs` の `PAGES` にも足してください。**
足さないと、その画面だけ誰にも見られないまま公開されます。

---

## コメントは日本語で

「何をしているか」ではなく **「なぜそうしているか」** を書きます。
`fitCanvas()` が `null` を返す理由のような、消すと同じ失敗をくり返すものが
書く価値のあるコメントです。

---

## これから

1. **いまここ** — デザインシステム＋画面（静的・ダミーデータ）
2. データをつなぐ — 認証／権限、予約・シフト、売上と報酬の自動計算
3. AIと集客 — 店長AI・オーナーAI・本部AI、最後に GLOWZ Market

**Market（集客プラットフォーム）を最初に作らない**のがこの順番の要点です。
店舗が使う理由（経営が楽になる）を先に作らないと、載せる店舗が集まりません。
